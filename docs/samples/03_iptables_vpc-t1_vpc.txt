# Sample expected output: `sudo iptables -S vpc-t1_vpc`
# (example lines added by vpcctl; your allowed CIDRs and comments may differ)
-N vpc-t1_vpc
-A vpc-t1_vpc -s 10.30.1.0/24 -d 10.40.1.0/24 -j ACCEPT -m comment --comment "vpcctl:peer t1_vpc<->t2_vpc"
-A vpc-t1_vpc -s 10.30.0.0/16 -d 10.30.0.0/16 -j ACCEPT -m comment --comment "vpcctl:intra"

# If NAT is enabled you will also see MASQUERADE rules in the nat table; list them with
# sudo iptables -t nat -S
